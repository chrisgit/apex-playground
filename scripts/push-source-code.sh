# Pushes the source code to the org named apex-playground
# If using default org which is the easiest method for pushing you do not need to specify the alias
sfdx force:source:push -u apex-playground --forceoverwrite